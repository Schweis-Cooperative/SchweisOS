# This file is owned by schweisos-mirrorlist.
#
# Bootstrap status: no production SchweisOS repository or mirror exists yet.
# The active entry below is a local development bootstrap endpoint. It is not a
# production mirror, public publication endpoint, or trust anchor.
#
# To use it, a developer or build host must explicitly stage an already-signed
# SchweisOS repository at:
#
#   /var/lib/schweisos/local-repo/$repo/os/$arch
#
# Until signed packages, signed repository databases, and approved public keys
# exist, this endpoint remains fail-closed under Required TrustedOnly policy.
#
# Arch Linux mirror entries belong to Arch's mirrorlist and must never be added
# here. Community mirrors require a separately documented admission policy.

Server = file:///var/lib/schweisos/local-repo/$repo/os/$arch
