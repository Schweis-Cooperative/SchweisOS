# SPDX-License-Identifier: GPL-3.0-or-later

# The source is the already-mounted, signature-verified live SquashFS. A
# package-owned reconciliation step removes exact live-only state afterwards.
unpack:
  - source: "/run/archiso/airootfs/"
    sourcefs: "file"
    destination: ""
